#include<bits/stdc++.h>

using namespace std;

void solv(vector<int> v,int n){
    
}

int main(){
    int n;
    while(1) {
        cin>>n;
        if(!n)break;
        vector<int> v(n);
        for(int i=0;i<n;i++){
            cin>>v[i];
        }
        solv(v,n);
    }
}